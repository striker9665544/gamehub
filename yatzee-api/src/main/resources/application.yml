spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gamehub
    username: root
    password: yourpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
  application:
    name: GameHubAuthService
